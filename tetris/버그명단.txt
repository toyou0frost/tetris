1: 하나의 블록이 아닌 4개의 블록으로 인식해서 한쪽 벽으로 블록을 밀 시 블록모양이 변형되는 버그
//2: 간혈적으로 블록 아래에 공간이 있어도 멈추는 현상 (아래에 이미 고정되어있어야 하는 블록이 움직일 경우 아래에 있는 블록이 이미 걸려있어서 발생하는 현상) -> 5번 버그 고치면 해결될 듯?
3: 블록을 아래부터 검사해서 가장 상단의 블록이 다른 블록에 걸릴경우 하단에있는 블록은 내려가고 최상단의 블록은 멈추어 블록의 형태가 망가지는 현상
4: gameover조건에 해당되어도 gameover함수로 이동하지 않는 현상(if문 조건 다시)
//5: 현재 내려오고있는 블록과 같은 색상의 이미 고정되어있는 블록이 방향키를 누를때 움직이는 현상(키다운 이벤트 문제)
6: 콘솔창 오류(setInterval이 자동으로 실행되는건데 함수명 지정해놓고 사용해서 그런듯? 콘솔창에는 BlockFall이 선언되어있지 않아서 그렇다는데 실제로 BlockFall를 빼버리면 콘솔오류는 없어지지만 실제 코드는 문제생기면서 블록들이 딜레이없이 나옴 )

해결된 버그 : 2, 5

블록 이동시 공백의 className도 변경